name: Continuous Integration

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  GO:
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-22.04, ubuntu-22.04-arm]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24.9'
      - run: sudo apt update && sudo apt install -y libsystemd-dev
      - name: gofmt -l .
        run: files=$(gofmt -l .); if [[ -n "$files" ]]; then echo "$files"; exit 1; fi
      - name: goimports -l .
        run: |
          go install golang.org/x/tools/cmd/goimports@latest
          files=$(goimports -l .); if [[ -n "$files" ]]; then echo "$files"; exit 1; fi
      - run: go vet ./...
      - run: go test ./...
      - run: go build -mod=readonly .

  # NOTE: the upstream Windows job was removed here. The Windows agent
  # (cmd/coroot-windows-agent, windows/, install.ps1, windows/wix/) is
  # intentionally left un-rebranded and out of scope for this pass, and its
  # internal imports still point at the old module path, so it no longer
  # builds against this module. Restore a Windows job once that code gets
  # its own rebranding pass.
