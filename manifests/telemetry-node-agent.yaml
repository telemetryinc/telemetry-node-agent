apiVersion: v1
kind: Namespace
metadata:
  name: telemetry

---

apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: telemetry-node-agent
  name: telemetry-node-agent
  namespace: telemetry
spec:
  selector:
    matchLabels:
      app: telemetry-node-agent
  template:
    metadata:
      labels:
        app: telemetry-node-agent
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '80'
    spec:
      tolerations:
        - operator: Exists
      hostPID: true
      containers:
        - name: telemetry-node-agent
          image: ghcr.io/telemetryinc/telemetry-node-agent
          args: ["--cgroupfs-root", "/host/sys/fs/cgroup"]
          ports:
            - containerPort: 80
              name: http
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /host/sys/fs/cgroup
              name: cgroupfs
              readOnly: true
            - mountPath: /sys/kernel/debug
              name: debugfs
              readOnly: false
      volumes:
        - hostPath:
            path: /sys/fs/cgroup
          name: cgroupfs
        - hostPath:
            path: /sys/kernel/debug
          name: debugfs
